import { SendWithData } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/SendWithData', function() {
  it(
    'SendWithData() should return a properly configured JSON-RPC request',
  )
})
