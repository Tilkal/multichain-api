import { CreateRawSendFrom } from '../../src/Commands'

describe('/CreateRawSendFrom()', function() {
  it(
    'should return a properly configured JSON-RPC request for the createrawsendfrom API command',
  )
})
