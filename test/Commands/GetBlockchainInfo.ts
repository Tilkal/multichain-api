import { GetBlockchainInfo } from '../../src/Commands'

describe('/GetBlockchainInfo()', function() {
  it(
    'should return a properly configured JSON-RPC request for the getblockchaininfo API command',
  )
})
