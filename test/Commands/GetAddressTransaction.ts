import { GetAddressTransaction } from '../../src/Commands'

describe('/GetAddressTransaction()', function() {
  it(
    'should return a properly configured JSON-RPC request for the getaddresstransaction API command',
  )
})
