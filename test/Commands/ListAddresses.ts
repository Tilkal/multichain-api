import { ListAddresses } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/ListAddresses()', function() {
  it(
    'should return a properly configured JSON-RPC request for the listaddresses API command',
  )
})
