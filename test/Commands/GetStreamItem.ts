import { GetStreamItem } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/GetStreamItem()', function() {
  it(
    'should return a properly configured JSON-RPC request for the getstreamitem API command',
  )
})
