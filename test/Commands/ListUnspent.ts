import { ListUnspent } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/ListUnspent', function() {
  it(
    'ListUnspent() should return a properly configured JSON-RPC request',
  )
})
