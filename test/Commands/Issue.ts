import { Issue } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/Issue()', function() {
  it(
    'should return a properly configured JSON-RPC request for the issue API command',
  )
})
