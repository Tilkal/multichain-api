import { ListStreamKeys } from '../../src/Commands'

describe('/ListStreamKeys()', function() {
  it(
    'should return a properly configured JSON-RPC request for the liststreamkeys API command',
  )
})
