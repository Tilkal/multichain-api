import { GetMemPoolInfo } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/GetMemPoolInfo()', function() {
  it(
    'should return a properly configured JSON-RPC request for the getmempoolinfo API command',
  )
})
