import { CreateFrom } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/ListUpgrades', function() {
  it(
    'ListUpgrades() should return a properly configured JSON-RPC request',
  )
})
