import { CreateRawTransaction } from '../../src/Commands'

describe('/CreateRawTransaction()', function() {
  it(
    'should return a properly configured JSON-RPC request for the createrawtransaction API command',
  )
})
