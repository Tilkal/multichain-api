import { GrantWithData } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/GrantWithData', function() {
  it(
    'GrantWithData() should return a properly configured JSON-RPC request',
  )
})
