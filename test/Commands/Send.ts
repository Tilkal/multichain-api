import { Send } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/Send', function() {
  it(
    'Send() should return a properly configured JSON-RPC request',
  )
})
