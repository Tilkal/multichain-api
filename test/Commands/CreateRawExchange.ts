import { CreateRawExchange } from '../../src/Commands'

describe('/CreateRawExchange()', function() {
  it(
    'should return a properly configured JSON-RPC request for the createrawexchange API command',
  )
})
