import { ImportPrivKey } from '../../src/Commands'

describe('/ImportPrivKey()', function() {
  it(
    'should return a properly configured JSON-RPC request for the importprivkey API command',
  )
})
