import { GetNewAddress } from '../../src/Commands'

describe('/GetNewAddress()', function() {
  it(
    'should return a properly configured JSON-RPC request for the getnewaddress API command',
  )
})
