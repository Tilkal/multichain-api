import { GetPeerInfo } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/GetPeerInfo()', function() {
  it(
    'should return a properly configured JSON-RPC request for the getpeerinfo API command',
  )
})
