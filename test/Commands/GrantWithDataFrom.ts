import { GrantWithDataFrom } from '../../src/Commands'

describe('/GrantWithDataFrom()', function() {
  it(
    'should return a properly configured JSON-RPC request for the grantwithdatafrom API command',
  )
})
