import { DumpPrivKey } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/DumpPrivKey', function() {
  it(
    'DumpPrivKey() should return a properly configured JSON-RPC request',
  )
})
