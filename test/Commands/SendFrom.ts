import { SendFrom } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/SendFrom', function() {
  it(
    'SendFrom() should return a properly configured JSON-RPC request',
  )
})
