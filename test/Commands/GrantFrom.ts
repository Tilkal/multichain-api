import { GrantFrom } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/GrantFrom()', function() {
  it(
    'should return a properly configured JSON-RPC request for the grantfrom API command',
  )
})
