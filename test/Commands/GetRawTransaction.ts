import { GetRawTransaction } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/GetRawTransaction', function() {
  it(
    'GetRawTransaction() should return a properly configured JSON-RPC request',
  )
})
