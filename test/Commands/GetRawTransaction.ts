import { GetRawTransaction } from '../../src/Commands'

describe('/GetRawTransaction()', function() {
  it(
    'should return a properly configured JSON-RPC request for the getrawtransaction API command',
  )
})
