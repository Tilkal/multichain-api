import { Create } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/Create()', function() {
  it(
    'should return a properly configured JSON-RPC request for the create API command',
  )
})
