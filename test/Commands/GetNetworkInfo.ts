import { GetNetworkInfo } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/GetNetworkInfo()', function() {
  it(
    'should return a properly configured JSON-RPC request for the getnetworkinfo API command',
  )
})
