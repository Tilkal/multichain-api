import { LockUnspent } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/LockUnspent', function() {
  it(
    'LockUnspent() should return a properly configured JSON-RPC request',
  )
})
