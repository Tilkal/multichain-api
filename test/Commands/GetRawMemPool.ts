import { GetRawMemPool } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/GetRawMemPool()', function() {
  it(
    'should return a properly configured JSON-RPC request for the getrawmempool API command',
  )
})
