import { AddNode } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/AddNode()', function() {
  it(
    'should return a properly configured JSON-RPC request for the addnode API command',
  )
})
