import { GetTxOut } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/GetTxOut()', function() {
  it(
    'should return a properly configured JSON-RPC request for the gettxout API command',
  )
})
