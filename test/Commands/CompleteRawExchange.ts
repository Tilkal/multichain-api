import { CompleteRawExchange } from '../../src/Commands'

describe('/CompleteRawExchange()', function() {
  it(
    'should return a properly configured JSON-RPC request for the completerawexchange API command',
  )
})
