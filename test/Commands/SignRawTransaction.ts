import { SignRawTransaction } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/SignRawTransaction', function() {
  it(
    'SignRawTransaction() should return a properly configured JSON-RPC request',
  )
})
