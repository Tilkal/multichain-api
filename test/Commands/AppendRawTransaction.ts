import { AppendRawTransaction } from '../../src/Commands'

describe('/AppendRawTransaction()', function() {
  it(
    'should return a properly configured JSON-RPC request for the appendrawtransaction API command',
  )
})
