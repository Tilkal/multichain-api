import { AppendRawTransaction } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/AppendRawTransaction', function() {
  it(
    'AppendRawTransaction() should return a properly configured JSON-RPC request',
  )
})
