import { GetRuntimeParams } from '../../src/Commands'

describe('/GetRuntimeParams()', function() {
  it(
    'should return a properly configured JSON-RPC request for the getruntimeparams API command',
  )
})
