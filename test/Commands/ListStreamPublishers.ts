import { ListStreamPublishers } from '../../src/Commands'

describe('/ListStreamPublishers()', function() {
  it(
    'should return a properly configured JSON-RPC request for the liststreampublishers API command',
  )
})
