import { CombineUnspent } from '../../src/Commands'

describe('/CombineUnspent()', function() {
  it(
    'should return a properly configured JSON-RPC request for the combineunspent API command',
  )
})
