import { Grant } from '../../src/Commands'
import { TestCommand } from '../test-helpers'

describe('/Grant()', function() {
  it(
    'should return a properly configured JSON-RPC request for the grant API command',
  )
})
